steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-f', 'Dockerfile.sync', '-t', 'gcr.io/${_PROJECT_ID}/update-companies-consolidated-sync${_ENV_SUFFIX}', '.']
images:
  - 'gcr.io/${_PROJECT_ID}/update-companies-consolidated-sync${_ENV_SUFFIX}'
